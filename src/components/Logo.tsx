import React from 'react'
import styled from 'styled-components'

const LogoText = styled.h1`
  margin: 0;
  margin-bottom: 40px;
  text-transform: uppercase;
`

const Logo = () => {
  return <LogoText>regular expression patterns</LogoText>
}

export default Logo
